        // Firebase web-app config. Paste the config object from
        // Firebase console → Project settings → Your apps → SDK setup.
        //
        // This is safe to commit: the apiKey is a public project identifier,
        // not a secret — access control lives entirely in the Firestore
        // security rules (email-allowlisted). While this is null, the app
        // runs in local(storage)-only mode everywhere.
        const FIREBASE_CONFIG = null;
